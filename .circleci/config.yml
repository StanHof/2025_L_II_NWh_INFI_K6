version: 2.1

jobs:
  build-and-test:
    machine:
      image: ubuntu-2004:202104-01
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: make deps
      - run:
          name: Run tests
          command: make test

workflows:
  basic:
    jobs:
      - build-and-test
